---
type: concept
domain: ai/rag
tags: [RAG, Advanced-RAG, Hybrid-Search, Rerank, AI-Agent]
status: done
created: 2026-09-16
updated: 2026-09-16

site_meta:
  id: advanced-rag
  name: 高级 RAG
  shortDesc: 检索前后全链路优化：查询改写、混合检索、重排压缩；工业界主流。
  icon: sliders
  difficulty: int
  tags: [RAG, Advanced-RAG, Hybrid-Search, Rerank]
  techFilters: [Qdrant, FastAPI]
  accent: '#38bdf8'
  enabledTools: [rag]
  rag_scheme: advanced
  prompts:
    - 发票提交时限是多少天？
    - 出差每天餐补标准是多少？
    - 我在外地出差，吃饭的钱怎么报销？
    - 请假扣不扣工资？
    - 新员工入职要办哪些手续？

publish: true
site: rag
cardId: advanced-rag
---

# 高级 RAG

> 检索前后全链路优化：查询改写、混合检索、重排压缩；工业界主流。

## 概述

Advanced RAG（高级 RAG）不是一种新技术，而是对 Naive RAG 的一整套**工程化补丁**：在「检索前」与「检索后」各加一个优化层，把检索链路做成**全链路优化**。它是目前工业界落地最主流的形态——本项目即属于这一类。

```
Naive：  切块 → 向量检索 → 拼接生成
Advanced：切块 → 向量化/索引(更优的切块和索引策略)
         [检索前]查询改写/意图分类 → [检索中]混合多路召回 → 融合
         → [检索后]Rerank 精排/上下文压缩 → 生成
```

## 为什么需要

Naive RAG 的三个致命缺陷：**检索噪声多、无关键词命中、上下文被污染**。Advanced RAG 逐一打补丁，且不改变「先检索后生成」的整体结构——**它依然是固定管道，只是每一环都被打磨过**。

## 检索前优化（让「问法」更容易命中语料）

| 手段 | 做什么 | 适用 |
|------|--------|------|
| **查询改写** | 把口语化/含混的问题改写成更贴近语料的表达 | 用户问法与文档表述不一致 |
| **意图分类 / 路由** | 先判断问题类型，决定查哪个知识库、走哪条检索路径 | 多知识源、多问题类型 |
| **Multi-Query** | 一个问题拆成多个子查询分别检索，再汇总去重 | 查询意图多样、单次检索覆盖不全 |
| **HyDE** | 先让 LLM 生成一段「假设答案」，用它的向量去检索 | 查询与答案表达差异大（详见 [rag-variants.md](rag-variants.md)） |

## 检索中优化（让「召回路径」更丰富）

- **混合检索（Hybrid Search）**：向量语义检索 + BM25 关键词检索**并行召回**，互相补短——向量负责「同义不同词」的语义匹配，BM25 命中专有名词、编号、长尾实体；
- **多路融合**：两路结果合并去重，常用 **RRF（Reciprocal Rank Fusion，倒数排名融合）**：按名次倒数求和，融合不同检索器的排名信号；
- **元数据过滤**：按文档属性（作者 / 时间 / 部门 / 权限）先过滤再检索，减少误召回与越权。

## 检索后优化（让「上下文」更干净）

- **Rerank 精排**：用更强的交叉编码器（Reranker）对初检 Top-K 逐对（query, 片段）二次打分，把真正相关的顶到前面——初检要求「广」（大 Top-K），精排要求「准」（小 Top-K）；
- **上下文压缩**：过滤低相关片段、压缩冗余信息，防止「上下文窗口污染」稀释有效注意力，降低 token 成本。

## 收益与边界

**收益**

- 召回更全、更准：混合检索 + 多路融合显著提升召回率；
- 上下文更干净：精排 + 压缩让喂给模型的片段质量更高；
- 精度提升立竿见影，是**性价比最高的落地形态**。

**边界 / 局限**

- **延迟叠加**：每一层优化都是一次额外调用，效果越好延迟越高，实时性场景需在效果与速度间权衡；
- **仍是固定管道**：假设「所有问题都检索一次、结果都可用」，无法按问题复杂度自适应；
- **关系盲区仍在**：混合检索本质仍是「文本匹配」，跨文档实体关联、多跳问题依然无解（需 Graph RAG）。

## 推荐 Prompt

Advanced RAG 是**固定管道**，其中需要 LLM 参与（可 Prompt 化）的只有「检索前改写」与「最终生成」两环；混合检索 / RRF 融合 / Rerank / 压缩都是**算法实现，无 Prompt**（用「每层优化」模式，而非「每环都问一次模型」）。

### 1. 查询改写（Multi-Query）

**作用**：把口语化/含混问题改写成多个更贴近制度语料的检索变体，弥补「单次查询覆盖不全」与「问法与原文不一致」的缺陷（本项目 naive/advanced 对比演示的就是这一环）。

**模型参数**：`qwen3.5-flash / temperature=0.3 / max_tokens=200 / enable_thinking=False`

**示例 Prompt**：

```
你是企业知识库检索助手。请把用户的问题改写成 3 个
更适合检索企业制度语料的查询变体：保留关键实体与数字，
用更正式、贴近规章制度原文的措辞；若原问题已足够正式，可做关键词化压缩。
每个变体单独一行输出，不要编号、不要解释、不要其他文字。
```

**要点**：每行一个变体、无编号无解释；返回时**始终把原始查询放首位**保证基础召回；未配聊天 Key 时回退规则化改写（去客套语 + 关键词变体）。

### 2. 生成作答（注入式，全方案共用）

RAG 的最终回答**不额外调用「RAG 生成模型」**，而是把检索命中（去重 → 精排后的 Top-K）注入主对话的用户消息（注入式增强）。推荐注入模板：

```
{用户原始问题}

【知识库检索结果（{方案名}）】
[相关度 0.91] {命中片段1}
[相关度 0.87] {命中片段2}
...

{指令}
```

- 检索充分时：`请优先基于以上检索内容回答；若内容不足以回答，再结合自身知识补充。`
- 需要严格引用时：`请基于以上检索内容回答，并标注内容对应的来源片段。`

> 若你要做**按问题复杂度选策略**（该不该检索、要不要改写、要不要多跳），那是 Modular 的「前置语义路由」职责——路由 Prompt 见 [modular-rag.md](modular-rag.md)「推荐 Prompt（按分支）」第 1 节。

## 本项目的做法

本项目把 RAG 做成**多方案可选框架**：同一份内置语料按方案写入各自独立的集合，侧边栏可随时切换方案，直观对比检索与回答差异。当前已落地 **naive / advanced / modular / agentic** 四方案，本卡聚焦 advanced 链路：

### 1. 方案隔离：同一语料、不同库

- 每个方案一个独立集合：`knowledge_naive`、`knowledge_advanced`（集合名 = `{prefix}_{scheme_id}`），互不干扰；
- 存储层抽象「存储后端」接口，**Qdrant 与 Elasticsearch 均已实现**（同一接口，配置改一行 `RAG_STORE_BACKEND` 即可无缝切换，见下文 §3）；
- 未配置后端或连接失败时回退内存检索，离线 / 测试也能跑通。

### 2. Advanced 方案（已落地）：检索前后全链路优化

- **入库拆分**：语义分块——句子边界感知 + Embedding 相似度贪心合并（块尾重叠续接上一句），完整保留嵌套制度规则，规避固定切块的语义断裂；
- **检索前（Query 重写）**：接入百炼聊天模型做 Multi-Query 改写（一次生成多个更贴近语料的查询变体），并**关闭思考模式**（改写是廉价的结构化任务，关闭 reasoning 省时且避免污染「每行一个变体」的纯文本输出）；未配聊天 Key 时回退规则化改写（去客套语 + 关键词变体；发票时限类问题还会补齐「发票提交时限规定 / 发票报销截止日期规定 / 发票上交流程及时限要求」等变体，对齐语料里的「发票提交时限：…15 个自然日内上传 OA」表述）；
- **检索中（多路召回）**：每个查询变体分别走**稠密语义路**（纯向量 Top-K）与**混合路**（Qdrant 稠密+稀疏 `Fusion.RRF` / ES `BM25`）两条互补路径；advanced 更进一步做**跨后端多路召回**——同时查询 **Qdrant（向量库，负责语义匹配）** 与 **Elasticsearch（关键词 BM25，负责专有名词/编号/精确表达）**，各路结果**按文本去重合并（保留最高分）**形成宽候选集；
- **检索后（重排序）**：DashScope `qwen3-rerank` 交叉编码器对候选精排（失败回退本地词法重排），把真正相关的片段顶到前面，过滤检索噪声；
- 检索事件携带 `rewrites`（重写变体）与 `reranked`（是否重排），前端检索卡片可直观看到 Advanced 的全链路处理过程。

### 3. 检索后端：多路召回 = ES（关键词）+ 向量库（语义）

- **naive**：单后端检索——默认 Qdrant（纯稠密余弦 Top-K），未配置时回退内存；
- **advanced**：**跨后端多路召回**——主后端默认 **Qdrant**（稠密向量负责语义匹配），并**叠加 Elasticsearch** 的 `text` BM25 关键词路（负责专有名词 / 编号 / 精确表达命中），两路结果**按文本去重**后进入重排；主后端可通过 `RAG_STORE_BACKEND=qdrant|elasticsearch|memory` 切换（主 ES 时叠加 Qdrant）；
- 两后端实现同一存储后端抽象：Qdrant `dense`+`sparse` 命名向量 `Prefetch` + `Fusion.RRF`；ES 8.x `dense_vector` kNN + BM25 `rank.rrf`；**ES <8.x 兼容路径**退化为纯 `BM25`（旧服务器无 dense_vector / kNN，与 Qdrant 稠密路互补）；
- 流水线：**召回（向量库 + ES 多路）→ 去重（跨后端 / 跨查询变体按文本去重，保留最高分）→ 重排（qwen3-rerank 精排，失败回退词法）**，把「宽召回」压缩为「准 Top-K」。

> **关于多后端的价值（重要）**：本项目内置语料规模小，Qdrant 与 ES 在小语料上召回质量几乎无差别——ES 路的工程价值主要体现在**大规模语料**场景（分布式分片、全文 BM25 + 向量混合、与既有 ES 数据栈打通）。因此「跨后端多路召回」更多是**架构与扩展性储备**；验证检索优化效果请对比 **naive / advanced 两方案**（方案差异立竿见影），而不是对比单后端差异。

### 4. 切块与索引策略：按项目选型，非一成不变

本项目当前用**语义分块**（句子边界 + 相似度合并），但这只是众多「入库拆分 / 索引策略」中的一种。**具体用哪种策略，必须按实际项目的文档形态、查询分布与成本预算来设计，不存在万能答案**：

| 策略 | 思路 | 适用 / 权衡 |
|------|------|------------|
| **定长 / 语义分块** | 固定窗口切或按句、按语义合并 | 通用基线；语义分块需 Embedding 成本，但块边界更贴近语义 |
| **大小块策略（Parent-Child / Big-Small）** | 小块精检索、大块（父块）供生成——命中子块后回取父块完整上下文 | 长文答案需要完整段落上下文，避免小块割裂规则（本项目语料的嵌套制度规则正适合） |
| **头部摘要策略（Summary / Header）** | 为文档 / 章节预生成摘要或抽取标题，作为检索入口；命中摘要后回取正文 | 长文档、目录式文档；额外一次 LLM 摘要成本，但对「章节级」问题命中率高 |
| **预生成问题策略（Pre-generated Questions）** | 为每个块预生成「可能被问到的问题」，用问题向量建索引，用户提问直接命中问题再回取块 | FAQ / 问答型知识库；把「答案导向」转为「问题导向」，显著提升口语化问法命中率 |
| **元数据 / 权限过滤** | 按部门、时间、作者、权限预过滤再检索 | 多租户、越权隔离场景；检索前先缩小候选集 |

> 这些策略可以**组合叠加**（如「预生成问题 + 大小块」），也可以**按方案隔离**（本项目每个 RAG 方案一个独立集合，正好用于对照不同拆分/索引策略的召回差异）。选择依据是：**你的问题长什么样、文档长什么样、可接受多大成本**。

### 5. 直观对比

- 侧边栏「RAG 方案」选择器（朴素 RAG / 高级 RAG），同一问题换方案即可看到检索卡片与回答差异；
- 检索事件带 `scheme` 字段，前端在检索卡片上标注当前方案与重写/重排过程，便于对照。

### 与通用设计的对应关系

| 通用设计 | 本项目做法 |
|---------|-----------|
| 方案隔离 | 同一语料、每方案独立集合（`knowledge_{scheme_id}`） |
| 向量化 + 索引 | DashScope `text-embedding-v3` 稠密 + 本地 n-gram 稀疏（不调用云端稀疏模型；Qdrant 命名向量 / ES `dense_vector`） |
| 混合检索 | advanced 跨后端：Qdrant 稠密+稀疏 `Fusion.RRF` 与 ES BM25 并行召回 → 按文本去重 → qwen3-rerank 精排 |
| 存储解耦 | 存储后端抽象接口，Qdrant / Elasticsearch 双实现（配置切换） |
| 生成 | 拼装检索上下文 + 用户问题，交给 LLM 作答 |

## 演进方向

- **需要组件可复用、按需组合** → [modular-rag.md](modular-rag.md)（模块化）；
- **需要跨实体理解、多跳推理** → [graph-rag.md](graph-rag.md)（图谱）；
- **需要自主决策、多轮迭代检索** → [agentic-rag.md](agentic-rag.md)（智能体）。
