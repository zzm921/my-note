---
type: moc
domain: AI/ML
tags: [机器学习, 学习日志, 记录]
status: living
created: 2026-09-16
updated: 2026-09-16
subject: 机器学习
---

# 机器学习 · 学习日志

> 追加式流水记录，**只加不改**。每次学完追加一行：日期 / 阶段 / 内容 / 产出 / 卡点。
> 周末复盘时我读这张表判断里程碑进度。

记录格式：

```
| 日期 | 阶段 | 学了什么 | 产出（文件/笔记） | 卡点 / 耗时 |
```

## 日志

| 日期 | 阶段 | 学了什么 | 产出（文件/笔记） | 卡点 / 耗时 |
|---|---|---|---|---|
| 2026-09-16 | M0 | 吴恩达机器学习：线性回归 + 逻辑回归（课程） | [[2-线性回归模型]]、[[4-分类与逻辑回归]]（后者已上站） | — |
| 2026-09-16 | M1 | NumPy / Pandas / Matplotlib 三件套 | 仓库 `machine-learning`：`01_numpy/` `02_pandas/` `03_matplotlib/` | — |
| 2026-09-16 | M2 | 从零实现：线性回归（标准化 + 批量梯度下降 + 代价曲线）、逻辑回归、正则化逻辑回归（多项式特征 + λ=0/1/100 对比） | 仓库 `machine-learning`：`04_linear_regression/` `05_logistic_regression/`（.py + .ipynb + 课程数据集） | 见下方「M2 验收结果」 |

## M2 验收结果（2026-09-16，服务器实跑 / **不依赖 sklearn**）

验收对照件全部用**手写可复现**的方式：线性回归对**正规方程解析解**，逻辑回归对**有限差分数值梯度**。

| 验收项 | 标准 | 实测 | 结论 |
|---|---|---|---|
| 损失随迭代单调下降 | 无上升步 | 1500 次迭代 32.0727 → 4.4770，最大步长 −4.4e−14（数值噪声） | ✅ 通过 |
| 与**正规方程解析解**的系数差 | < 1e−2 | θ_梯度下降 `[5.839133, 4.593040]` vs θ_正规方程 `[5.839135, 4.593041]` → **1.66e−06**；最终代价两者都是 4.47697138 | ✅ 远超标准 |
| 逻辑回归**梯度实现正确性** | 解析 vs 有限差分差 < 1e−6 | 最大差 **3.27e−11** | ✅ 通过 |
| 逻辑回归**收敛程度** | 最优处梯度范数 ≈ 0 | 梯度范数 **6.75e−02** | ⚠️ **未完全收敛**（1500 次迭代/步长不够，还能再降） |
| 训练集准确率 | ≥ 课程参考 0.89 | **0.9000** | ✅ 达到课程水平 |
| 特征缩放的收敛收益 | 可复现演示 | 自己未做；服务器给出可复现做法：α=0.005 时未标准化需 **2292** 次迭代才到代价 4.50，标准化只需 **707** 次；未标准化 α=0.05 **直接发散** | ️ 待补进代码 |
| 可复现性（clone 即跑） | 无报错 | `04` 缺 `seaborn`；`05/regularized` 相对路径 → 从仓库根运行 FileNotFoundError | ❌ 待修 |

> 验收脚本已留档：[[验收脚本-M2.py]]（只需 numpy + 课程数据，独立于任何建模库）

## 阶段小结（周末复盘时填）

| 周次 | 里程碑 | 是否达标 | 说明 |
|---|---|---|---|
| 2026-W38 | M1 | ✅ 达标 | 三件套完成，示例代码能跑通 |
| 2026-W38 | M2 | 🔵 基本达标 | 核心数值正确性极好（1.66e−06 / 3.27e−11）；待补：逻辑回归收敛调优、缩放对比演示、依赖与路径 |
| 2026-W38 | M3 | ⏳ 进行中 | 全手写版端到端流程（交叉验证 / 指标 / ROC-AUC / 正则化扫描），**不使用 sklearn** |