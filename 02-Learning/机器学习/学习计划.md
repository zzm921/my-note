---
type: moc
domain: AI/ML
tags: [机器学习, 学习计划, 吴恩达, 线性回归, 逻辑回归]
status: living
created: 2026-09-16
updated: 2026-09-16
subject: 机器学习
stage: M3
---

# 机器学习 · 学习计划

> 用吴恩达《机器学习》课程打地基，配「从零实现 → 完整流程 → 课程推进」三段实战。
> 目标：**不只是看懂公式，而是能独立跑通「数据 → 特征 → 训练 → 评估 → 调参」闭环，并产出可复用代码与笔记。**

## 一、起点与现状（2026-09-16）

| 项目 | 状态 |
|---|---|
| 吴恩达课程 · 线性回归 | ✅ 已学完（笔记：[[2-线性回归模型]]） |
| 吴恩达课程 · 逻辑回归 | ✅ 已学完（笔记：[[4-分类与逻辑回归]]，已上站） |
| NumPy / Pandas / Matplotlib | ✅ 已学完（M1 达标，代码 `01_numpy/` `02_pandas/` `03_matplotlib/`） |
| 编码练手（回归 / 分类） | ✅ 基本达标（M2：3/4 验收项通过，见 [[机器学习-学习日志]]） |
| 课程后续章节 | 神经网络 / 模型评估与调优 / 决策树 / 非监督 / 推荐 / 强化 —— 笔记已成稿，待实战验证 |

> 代码仓库：<https://github.com/zzm921/machine-learning>（986 行，含 .py + .ipynb + 课程数据集）

## 二、阶段目标

### M1 · 工具三件套 ✅ 已达标（2026-09-16）
**目标**：能独立用 NumPy 做向量化计算、用 Pandas 读写与清洗表格、用 Matplotlib 画四类常用图。

- 交付物：
  1. 3 个可运行脚本 → `10-AI/ml/练习/`（`01_numpy_basics.py`、`02_pandas_eda.py`、`03_matplotlib_plots.py`）
  2. 1 篇速查笔记 → `10-AI/ml/NumPy-Pandas-Matplotlib速查.md`（用教程卡模板）
  3. 学习日志 3 条
- **验收标准**（不看资料能做到）：
  - [ ] 用 NumPy 写出批量梯度下降迭代（20 行内，向量化、无 for 循环遍历样本）
  - [ ] 用 Pandas 读 CSV → 处理缺失值 → groupby 聚合 → 两表 merge
  - [ ] 用 Matplotlib 画「散点 + 拟合线」双子图，含标题/轴标签/图例

### M2 · 从零实现 🔵 基本达标（3/4 验收项通过，2026-09-16）
**目标**：不调库，用 NumPy 手写两种模型的训练过程，并与 sklearn 对照。

- 内容：线性回归（批量 / 随机梯度下降、特征缩放、学习率对比）、逻辑回归（Sigmoid、对数损失、决策边界）
- 交付物：`linear_regression_from_scratch.py`、`logistic_regression_from_scratch.py` + 1 篇笔记（站点卡片候选：`ml-linear-regression-from-scratch` / `ml-logistic-regression-from-scratch`）
- **验收标准**：
  - [ ] 损失随迭代单调下降（画曲线验证）
  - [ ] 与 sklearn 的系数/截距差异 < 1e-2
  - [ ] 逻辑回归在测试集准确率与 sklearn 差距 < 2%
  - [ ] 能解释「为什么需要特征缩放」并演示有无缩放的收敛速度差异

### M3 · 完整流程（第 3 周）
**目标**：拿一个真实数据集，独立走完一次端到端流程。

- 数据集建议：房价回归（回归线）或 泰坦尼克/鸢尾花（分类线）
- 内容：清洗 → 特征工程 → 训练/验证/测试切分 → 训练 → 评估 → 调参 → 可视化
- 评估指标：回归 `MSE / RMSE / MAE / R²`；分类 `混淆矩阵 / Precision / Recall / F1 / ROC-AUC`；交叉验证；L1/L2 正则化
- 交付物：项目脚本（或多文件小项目）+ 1 篇流程复盘笔记（站点卡片候选）
- **验收标准**：
  - [ ] 能说清「为什么汇报 R² 而不是只汇报 MSE」
  - [ ] 能演示正则化强度 λ 变大时系数如何变化，并解释偏差-方差权衡
  - [ ] 能画 ROC 曲线并解释阈值选择对 P/R 的影响

### M4 · 回到课程主线（第 4 周起）
**目标**：把课程剩余章节逐章推进，每章配一次 30 分钟级的 sklearn 实现练习。

- 顺序：5-神经网络 → 6-模型评估与调优 → 7-决策树 → 8-非监督学习 → 9-推荐系统 → 10-强化学习
- 每章交付：练习脚本 + 笔记校对（已有成稿，补上「代码验证」小节）
- **验收标准**：每章能用一句话说清「它解决什么问题、代价是什么」，并跑出对应 sklearn 最小示例

## 三、本周计划（M3 · 端到端流程，3 次 × 90 分钟）

**本周补三件之前没做过的东西**：交叉验证、ROC/AUC 与阈值权衡、正则化强度扫描（把 M2 里手写的 λ 对比升级成规范流程）。

| 次序 | 类型 | 具体动作 | 产出 |
|---|---|---|---|
| 第 1 次 | 学 | 交叉验证（K 折 / 分层 K 折）为什么要做、偏差-方差；ROC/AUC 的几何含义与阈值选择 | 笔记小节 + 能跑通的 K 折评估脚本 |
| 第 2 次 | 练 | 用 sklearn 内置数据集（离线可跑）跑回归 + 分类两条 pipeline：切分 → 标准化 → 训练 → 交叉验证 → 评估 → 残差图 / 混淆矩阵 / ROC | `06_pipeline/pipeline_regression.py`、`pipeline_classification.py` |
| 第 3 次 | 写 | 正则化强度扫描 + 学习曲线；整理复盘笔记（教程卡）→ 上站候选 | `tuning_regularization.py` + 笔记 1 篇 |

**交付物**
1. 新目录 `06_pipeline/`（三个脚本，**从仓库任意位置运行都能跑通**）
2. 一篇复盘笔记（`_Templates/②教程卡`）→ 站点候选 `ml-end-to-end-pipeline`
3. README 补「环境与运行方式」，`requirements.txt` 补全

**验收标准（下周复盘时逐条勾）**
- [ ] K 折交叉验证报告「均值 ± 标准差」，并能解释为什么比单次切分可靠
- [ ] 画出 ROC 曲线并算 AUC；演示阈值 0.3 → 0.7 时 Precision / Recall 如何变化
- [ ] 用 GridSearchCV 或手写扫描选出最优正则化强度，画出「正则化强度 → 验证集得分」曲线
- [ ] 能解释「训练集得分高、验证集低」意味着什么，并给出自己数据上的一个例子
- [ ] 三个脚本在仓库根目录运行均无报错（数据路径用 `Path(__file__).parent`）

**M2 收尾小任务（10 分钟，顺手做）**
- [ ] `requirements.txt` 加 `seaborn`（`04` 脚本 import 了它，否则 clone 后跑不起来）
- [ ] `05_logistic_regression/regularized_logistic_regression.py` 的数据路径改成 `Path(__file__).parent / "ex2data2.txt"`
- [ ] 线性回归补一个**严谨的**「标准化 vs 未标准化」收敛对比（比较达到同等代价所需迭代数，或展示未缩放时必须用极小 α 才不发散）——当前只有注释说明，没有可复现结论

### 留档：M1 周计划（已完成）

| 次序 | 类型 | 具体动作 |
|---|---|---|
| 第 1 次 | 学 | NumPy 核心：ndarray / 广播 / 切片 / 向量化 |
| 第 2 次 | 练 | Pandas + Matplotlib：读 CSV / groupby / merge / 四类图 |
| 第 3 次 | 写 | 速查笔记 + 搭 M2 线性回归骨架 |

> 每次结束在 [[机器学习-学习日志]] 追加一行；周末我做复盘并更新本文件。

## 四、节奏与约定

- **学 / 练 / 写** 三段轮转，缺一段则该次不算完成（只学不练会假懂，只练不写会遗忘）
- 代码统一放 `10-AI/ml/练习/`；笔记放 `10-AI/ml/`
- 里程碑达标 → 笔记考虑上站（`site: ml`），与现有 4 张课程卡片形成「课程 + 实战」两组
- **超时处理**：某阶段超过 1 周未达标，就把该阶段拆成「学」「练」两半，不跳步

## 五、需要的反馈（我据此更新计划）

每次学完告诉我三件事即可：

1. **学了什么**（章节 / 主题）
2. **做了什么**（代码 / 练习 / 产出文件）
3. **卡在哪**（不理解的点 / 报错 / 花时间超过预期的地方）

## 六、关联

- 课程笔记：[[机器学习-地图]]（10 篇成链）
- 学习日志：[[机器学习-学习日志]]
- 站点分区：`ml`（当前 4 张卡片）
- 总看板：[[学习总览]]